import "./style.css";
import { initializeApp } from "./appController";
import { initEvents } from "./eventManager";

initializeApp();
initEvents();

/* TODO's
Make date/time choseable when creating todo
Format date and time correctly
Make priority choseable when creating todo
Show date/time and priority in todo details
Make default project not deleteable
Make todos swapable
*/
